import React from 'react'

const MainTitle = ({title,secondTitle}) => {
  // const title = props.title;
  return (
    <div>
      <h2>{title} {secondTitle}</h2>
    </div>
  )
}

export default MainTitle